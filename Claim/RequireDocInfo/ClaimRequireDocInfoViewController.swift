//
//  ClaimRequireDocInfoViewController.swift
//  Bomapp
//
//  Created by rowkaxl on 26/02/2019.
//  Copyright (c) 2019 Redvelvet Ventures Inc. All rights reserved.
//
//  This file was generated by the Clean Swift Xcode Templates so
//  you can apply clean architecture to your iOS and Mac projects,
//  see http://clean-swift.com
//

import UIKit
import RxCocoa
import RxSwift

protocol ClaimRequireDocInfoDisplayLogic: class {
    func displaySomething(viewModel: ClaimRequireDocInfo.Something.ViewModel)
}

class ClaimRequireDocInfoViewController: UIViewController, ClaimRequireDocInfoDisplayLogic {
    var interactor: ClaimRequireDocInfoBusinessLogic?
    var router: (NSObjectProtocol & ClaimRequireDocInfoRoutingLogic & ClaimRequireDocInfoDataPassing)?

    // MARK: Object lifecycle

    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        super.init(nibName: nibNameOrNil, bundle: nibBundleOrNil)
        setup()
    }

    required init?(coder aDecoder: NSCoder) {
        super.init(coder: aDecoder)
        setup()
    }

    // MARK: Setup

    private func setup() {
        let viewController = self
        let interactor = ClaimRequireDocInfoInteractor()
        let presenter = ClaimRequireDocInfoPresenter()
        let router = ClaimRequireDocInfoRouter()
        viewController.interactor = interactor
        viewController.router = router
        interactor.presenter = presenter
        presenter.viewController = viewController
        router.viewController = viewController
        router.dataStore = interactor
    }

    // MARK: Routing

    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        if let scene = segue.identifier {
            let selector = NSSelectorFromString("routeTo\(scene)WithSegue:")
            if let router = router, router.responds(to: selector) {
                router.perform(selector, with: segue)
            }
        }
    }

    // MARK: View lifecycle

    override func viewDidLoad() {
        super.viewDidLoad()
//        doSomething()

        setRX()
    }

    private let disposeBag = DisposeBag()

    @IBOutlet weak var backBtn: UIButton!
    @IBOutlet weak var closeBtn: UIButton!

    // MARK: Do something

    //@IBOutlet weak var nameTextField: UITextField!

    func doSomething() {
        let request = ClaimRequireDocInfo.Something.Request()
        interactor?.doSomething(request: request)
    }

    func displaySomething(viewModel: ClaimRequireDocInfo.Something.ViewModel) {
        //nameTextField.text = viewModel.name
    }
}

extension ClaimRequireDocInfoViewController {

    func setRX() {

        self.backBtn.rx.tap
            .subscribe(onNext: {
                self.navigationController?.popViewController(animated: true)
            }).disposed(by: disposeBag)

        self.closeBtn.rx.tap
            .subscribe(onNext: {
                self.dismiss(animated: true)
            }).disposed(by: disposeBag)
    }
}
